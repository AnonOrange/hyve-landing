import LoadingScanner from '../../LoadingScanner';

export default function Loading() {
  return <LoadingScanner subtitle="LOADING FEED" message="Pulling audio + cameras + summary" accent="#00D4FF" />;
}
